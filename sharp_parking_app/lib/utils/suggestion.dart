class Suggestion {
  final String placeId;
  final String description;
  final String name;
  final String formattedAddress;
  final double lat;
  final double lng;

  Suggestion(this.placeId, this.name,this.description, this.formattedAddress, this.lat, this.lng);
}